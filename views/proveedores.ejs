<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Proveedores</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/styles/style_inicio.css">
</head>
<body>
  <%- include('partials/navbar_inicio') %>
    <%- include('partials/sidebar') %>
  <h1>Gestión de Proveedores</h1>
  <div class="container">
    <div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>ID</th>
        <th>Nombre</th>
        <th>Teléfono</th>
        <th>Ubicación</th>
        <th>Correo Contacto</th>
        <th>Acciones</th> <!-- Nueva columna para acciones -->
      </tr>
    </thead>
    <tbody>
      <% if (Proveedores.length > 0) { %>
        <% Proveedores.forEach(proveedor => { %>
          <tr>
            <td><%= proveedor.id_proveedores %></td>
            <td><%= proveedor.nombre %></td>
            <td><%= proveedor.telefono %></td>
            <td><%= proveedor.ubicacion %></td>
            <td><%= proveedor.correo_contacto %></td>
            <td>
              <!-- Enlace para editar -->
              <a href="/editar-proveedor/<%= proveedor.id_proveedores %>" class="btn btn-warning">Editar</a>
              <!-- Enlace para eliminar -->
              <a href="/eliminar-proveedor/<%= proveedor.id_proveedores %>" class="btn btn-danger" onclick="return confirm('¿Estás seguro de eliminar este proveedor?');">Eliminar</a>
            </td>
          </tr>
        <% }); %>
      <% } else { %>
        <tr>
          <td colspan="6">No hay proveedores registrados.</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
</div>
    <%- include('partials/footer') %>
</body>
</html>
